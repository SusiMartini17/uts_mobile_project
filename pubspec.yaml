name: uts_mobile_project
description: A Flutter project for UTS Pemrograman Mobile 2
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.1.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  url_launcher: ^6.1.7
  intl: ^0.20.2
  uuid: ^4.5.2
  flutter_lints: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/splash_bg.jpg
